import { StyleSheet } from "react-native";

export default StyleSheet.create({
  
    scrollViewContainer: {
        flexGrow: 1,
        padding: 20,
      },
      container: {
        flex: 1,
      },
      chartContainer: {
        alignItems: "center",
      },
      labelsStyle: {
        fontSize: 18,
        fontWeight: "bold",
        alignSelf: "center",
        marginVertical: 20,
      },
});
